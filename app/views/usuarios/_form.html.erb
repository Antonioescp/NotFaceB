<%= form_with model: @usuario, local: true do |f| %>

  <% if @usuario.errors.any? %>
  
    <div class="errores-usuario">
      <%= pluralize( @usuario.errors.count, "error" ) %>, revisa los campos
    </div>

    <ul>
      <% @usuario.errors.full_messages.each do |mensaje| %>
        <li><%= mensaje %></li>
      <% end %>
    </ul>

  <% end %>

  <div class="field">
    <%= f.label :nombre %> <br> 
    <%= f.text_field :nombre %>
  </div>

  <div class="field">
  	<%= f.label :apellido %> <br>
    <%= f.text_field :apellido %>
  </div>

  <div class="field">
  	<%= f.label :correo %> <br>
    <%= f.text_field :correo %>
  </div>
  
  <div class="field">
  	<%= f.label "Contraseña" %> <br>
    <%= f.password_field :password %>
  </div>

  <div class="submit">
  	<%= f.submit "Registrarme" %>
  </div>

<% end %>